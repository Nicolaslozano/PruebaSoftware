package test;

import org.junit.Test;

import modelo.Cliente;
import modelo.Factura;
import modelo.Medidor;


import java.time.LocalDate;

import static org.junit.Assert.assertEquals;

public class FacturaTest {

    @Test
    public void testCrearFactura() {
        /*
        Medidor medidor = new Medidor("M001");
        Cliente cliente = new Cliente(1, "Juan Perez", "Calle Falsa 123", "Residencial", medidor,18);
        Factura factura = new Factura("F001", cliente, 150.0);

        assertEquals("F001", factura.getIdFactura());
        assertEquals(cliente, factura.getCliente());
        assertEquals(150.0, factura.getMontoTotal());
        assertEquals(LocalDate.now(), factura.getFechaEmision());
    }

         */
    }
}
