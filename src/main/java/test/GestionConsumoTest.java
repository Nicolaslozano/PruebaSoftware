package test;

import modelo.Cliente;
import modelo.Medidor;
import servicios.GestionConsumo;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class GestionConsumoTest {

    @Test
    public void testRegistrarLectura() {
        /*
        GestionConsumo gestionConsumo = new GestionConsumo();
        Medidor medidor = new Medidor("M001");
        Cliente cliente = new Cliente(1, "Juan Perez", "Calle Falsa 123", "Residencial", medidor,18);

        double consumo = gestionConsumo.registrarLectura(cliente, 200.0);
        assertEquals(200.0, consumo);

        consumo = gestionConsumo.registrarLectura(cliente, 300.0);
        assertEquals(100.0, consumo);  // Consumo entre la última y nueva lectura debería ser 100 kWh
    }

         */
    }
}
